Strictly follow the steps. Never write code. Don't do any more or less than what's asked:

1. Generate a random number 1-100
2. Place the number in the variable `RANDOM`
3. Ask the user to guess the random number that you're thinking of.
4. Place their guess in `USER_GUESS`.
5. If their `USER GUESS` is the same number as `RANDOM` then exitedly tell them they guessed correctly. Otherwise, tell them whether it is higher or lower and go back to step 3.