Strictly follow the steps:

1. Prompt the user for their first and last name.

2. Put their first name in the variable `FIRST` and their last name in the variable `LAST`.

3. If `NAME` is a male name, put "male" in the variable `GENDER`.
    Else if `NAME` is a female name, put "female" in the variable `GENDER`.

4. If `GENDER` = male: `FORMAL TITLE` = "Mr."
    Else if `GENDER` = female: `FORMAL TITLE` = "Mrs."

5. Without writing any code or showing any reasoning, respond "Hello `FORMAL TITILE` `FIRST` `LAST`. You are a `GENDER`.
